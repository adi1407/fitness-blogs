/**
 * Legacy marketing home stack — kept as a thin re-export so older imports
 * still resolve. Live home uses HomeMagazine + HomeMagazineBands.
 */
export { HomeMagazineBands as HomePageContent } from "./HomeMagazineBands";
