import { ImageAutoSlider } from "@/components/ui/image-auto-slider";

/** Demo usecase for the infinite image auto-slider. */
export function DemoOne() {
  return <ImageAutoSlider className="min-h-[50vh]" />;
}
